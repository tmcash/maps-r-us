<!DOCTYPE html>
<html lang="en">

<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>User Registry</title>
{{!-- {{#if loged_in}} --}}
<link rel="stylesheet" href="./css/style.css">
<!-- framework -->
<script src="https://cdn.tailwindcss.com" defer ></script>
<script src="https://kit.fontawesome.com/337be747ec.js" crossorigin="anonymous"></script>
{{!-- {{/if}} --}}
</head>

<body>
    {{!-- {{#if logged_in}} --}}
    <header id="wrapper" class="center-aligned">
        <div class="journalimg" id="travel-journal" >
            <img src="./images/Travel-Journal.jpg" alt="MapsRUs logo">
        </div>

        <nav>
            <h1>Welcome</h1>{{user}}
            <button class="button" id="logout">logout</button>
        </nav>
    </header>
    <div class="section-container-options">
        <div class="options-container">
            <h2>🔍 Search</h2>
            <h2>📝 Itinerary</h2>
            <h2>📧 Email</h2>
        </div>
    </div>
    <main>
        <aside class="side-container">
            <div class="start-date-picker">
                <label for="start-date-input">Start Date:</label>
                <input type="date" id="start-date-input" name="start-date">
            </div>
            <div class="end-date-picker"> 
                <label for="end-date-input">End Date:</label>
                <input type="date" id="end-date-input" name="end-date">
            </div>

            <!-- drop down menu for city selection -->
                <div class="city-dropdown">
                <label for="cities">Choose a city:</label>

                <select name="city" id="city">
                </select>
            </div>
        <!-- Div containers for button selections  -->
            <div class="side">
                <section id="search-section" class="mb-2">
                <p class="text-xl">Make a Selection:</p>
                <button class="btn bg-teal-800 hover:bg-teal-400 hover:font-bold text-white text-lg" id="btn-hotel">Hotel</button>
                <button class="btn bg-teal-800 hover:bg-teal-400 hover:font-bold text-white text-lg" id="btn-food">Restaurants</button>
                <button class="btn bg-teal-800 hover:bg-teal-400 hover:font-bold text-white text-lg" id="btn-activity">Activities</button>

            </div>
        </aside>
        <!-- div container for results -->
        <section class="container" id="results-container">
            <div class="result-container">
                <div class="result-grid" id="result-grid-all">
                <!-- hotel/food/activity information here -->
                </div>
            </div>
        </section>
    </main>

    {{!-- {{/if}} --}}
<main class="container container-fluid mt-5">
    {{{ body }}}
</main>
<footer>
    
</footer>


{{!-- {{#if logged_in}} --}}
<script src="./js/index.js"></script>
<script src="./js/logout.js"></script>
{{!-- {{/if}} --}}
</body>


</html>